[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  PUBLIC_API_URL = "https://genegecko.onrender.com"

[[redirects]]
  from = "/api/*"
  to = "https://genegecko.onrender.com/api/:splat"
  status = 200
  force = true
